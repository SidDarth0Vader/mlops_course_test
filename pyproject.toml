[project]
name = "mlops-course-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "numpy>=2.0.2",
    "pandas>=2.3.2",
    "scikit-learn>=1.6.1",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
